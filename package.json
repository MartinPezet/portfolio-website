{
    "scripts": {
        "dev": "npm-run-all --parallel client signInServer",
        "client": "cd ./client && npm run dev",
        "signInServer": "cd ./signInServer && npm run dev"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5"
    }
}
